# 智能竞品分析多Agent系统 — 依赖清单
# pip install -r requirements.txt

# ======== 核心依赖 ========
requests>=2.28.0           # 千帆API + 百度搜索 HTTP调用（LLM/搜索模式必需）

# ======== LangGraph 工作流编排 ========
langgraph>=0.2.0            # LangGraph 核心编排框架
langchain>=0.3.0            # LangChain 生态基础
langchain-core>=0.3.0       # LangChain 核心组件

# ======== LangSmith 追踪可视化 ========
langsmith>=0.1.0            # LangSmith 全链路追踪（可选，但强烈推荐启用）
